# Cloudflare Tunnel 环境变量示例
# 复制到 .env.local 文件使用

# API 后端 - 通过 Caddy 代理，使用相对路径
VITE_API_BASE_URL=/api

# 语音服务 WebSocket - 通过 Caddy 代理，使用相对路径
# 浏览器会自动转换为 ws:// 或 wss://
VITE_VOICE_WS_URL=/ws/voice

# Google Maps API Key（保留原有的）
VITE_GOOGLE_MAPS_API_KEY=你的Google_Maps_API密钥

